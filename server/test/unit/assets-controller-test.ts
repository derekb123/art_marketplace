const app = require('../../app');
const AssetController = require('../../controllers/assets_controller')
const endpointUrl: string = '/assets/';

describe('AssetController.createAsset', () => {
  test('it should have a createAsset function')
});
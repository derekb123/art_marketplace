const app = require('../../app');
const NftController = require('../../controllers/nfts_controller')
const endpointUrl: string = '/nfts/';

describe('NftController.createNft', () => {
  test('it should have a createNft function')
});